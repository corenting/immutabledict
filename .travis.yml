language: python
os: linux

cache:
  pip: true
  directories:
    - $HOME/.cache/pypoetry

python:
  - "3.5"
  - "3.6"
  - "3.7"
  - "3.8"
  - "3.9-dev"
  - "pypy3"
before_install:
  - pip install --upgrade pip
  - pip install poetry
install:
  - poetry install
script:
  - if [[ $TRAVIS_PYTHON_VERSION != 3.5 ]]; then poetry run black --check .; fi
  - poetry run isort --check-only **/*.py
  - poetry run pytest tests --cov=immutabledict
after_success:
  - bash <(curl -s https://codecov.io/bash)

jobs:
  allow_failures:
    - python: "3.9-dev"
  include:
    - stage: deploy
      script: skip
      if: tag IS present
      python: "3.8"
      before_deploy:
        - poetry config pypi-token.pypi $PYPI_TOKEN
        - poetry build
      deploy:
        provider: script
        script: poetry publish
        skip_cleanup: true
        on:
          tags: true
